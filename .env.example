MONGO_URI=""        # Required.
DB_NAME=""          # Required.
JWT_SECRET=""       # Required. Must be at least 32 characters. Used to generate a secret key for JWT authentication.
SENDGRID_API_KEY="" # Required. To bypass email verification, set to the string literal "null"